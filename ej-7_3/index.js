// Iterar por todos los elementos dentro de un array utilizando .forEach y mostrarlos en 
// pantalla.

const myArray = [1, 2, 3, 4, 5, 6];

myArray.forEach( myArray => {
    document.write( `${myArray[cont]} <br>` );
} );
