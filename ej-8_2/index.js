// Convertir un array ingresado dentro del código en un string (existe un método en
// javascript para hacerlo).

const arrayFrutas = ['strawberry', 'orange', 'blueberry', 'blackberry'];

const frutas = arrayFrutas.join( ' ' );

console.log( frutas );
