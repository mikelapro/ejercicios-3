// Crear una función que reciba dos valores y devuelva la suma de ellos.

const num1 = 1;
const num2 = 2;

const getSuma = ( num1, num2 ) => {
    const resultado = num1 + num2;

    return resultado;
};

document.write( getSuma( num1, num2 ) );
